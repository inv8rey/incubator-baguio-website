// Baguio City centre — the default viewport for every map surface
// (LocationPicker today; the public ecosystem map when it ships).
export const BAGUIO_CENTER: [number, number] = [120.596, 16.4023];
